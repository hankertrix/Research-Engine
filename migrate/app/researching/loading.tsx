// The loading page for the researching page

// Mark this as a client component
"use client";

import LoadingPage from "../../components/LoadingPage";

export default function Loading() {
  return <LoadingPage text={ "Researching" } />;
}